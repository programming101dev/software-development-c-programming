#include <stdlib.h>

int main(int argc, char *argv[])
{
    int exit_status;

    if (argc == 0)
    {
        exit_status = EXIT_FAILURE;
    }
    else
    {
        exit_status = EXIT_FAILURE;
    }

    return exit_status;
}